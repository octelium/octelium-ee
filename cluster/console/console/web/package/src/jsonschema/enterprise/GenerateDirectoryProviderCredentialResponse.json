{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/GenerateDirectoryProviderCredentialResponse",
    "definitions": {
        "GenerateDirectoryProviderCredentialResponse": {
            "properties": {
                "bearer": {
                    "$ref": "#/definitions/octelium.api.main.enterprise.v1.GenerateDirectoryProviderCredentialResponse.Bearer",
                    "additionalProperties": false,
                    "description": "Bearer is the generated bearer credential."
                }
            },
            "additionalProperties": false,
            "type": "object",
            "allOf": [
                {
                    "oneOf": [
                        {
                            "not": {
                                "anyOf": [
                                    {
                                        "required": [
                                            "bearer"
                                        ]
                                    }
                                ]
                            }
                        },
                        {
                            "required": [
                                "bearer"
                            ]
                        }
                    ]
                }
            ],
            "title": "Generate Directory Provider Credential Response",
            "description": "GenerateDirectoryProviderCredentialResponse is the response of the GenerateDirectoryProviderCredential method."
        },
        "octelium.api.main.enterprise.v1.GenerateDirectoryProviderCredentialResponse.Bearer": {
            "properties": {
                "accessToken": {
                    "type": "string",
                    "description": "AccessToken is the access token that is set in the `Authorization` request header by the directory. It is only returned once upon its generation."
                }
            },
            "additionalProperties": false,
            "type": "object",
            "title": "Bearer",
            "description": "Bearer is the generated bearer credential"
        }
    }
}