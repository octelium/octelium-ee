{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/SynchronizeSecretStoreResponse",
    "definitions": {
        "SynchronizeSecretStoreResponse": {
            "additionalProperties": false,
            "type": "object",
            "title": "Synchronize Secret Store Response",
            "description": "SynchronizeSecretStoreResponse is the response of the SynchronizeSecretStore method."
        }
    }
}